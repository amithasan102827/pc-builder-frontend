import { useDispatch, useSelector } from 'react-redux';

// Use the useDispatch and useSelector hooks from react-redux
export const useAppDispatch = useDispatch;
export const useAppSelector = useSelector;